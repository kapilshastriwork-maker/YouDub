# Reference copy of FunAudioLLM/CosyVoice/requirements.txt.
# The live install runs against the cloned repo (see setup_colab.md):
#
#     git clone --recursive https://github.com/FunAudioLLM/CosyVoice.git
#     cd CosyVoice && pip install -r requirements.txt
#
# This file is kept here as a readable reference of the pinned versions
# the upstream project uses. DO NOT pip install -r cosyv_requirements.txt
# directly — the Linux-only `deepspeed` and `tensorrt-cu12` packages will
# fail on macOS/Windows and the `onnxruntime-gpu` package is not compatible
# with Colab's default CUDA 12.1 either way.
#
# Last synced from:
#   https://github.com/FunAudioLLM/CosyVoice/blob/main/requirements.txt

# --- Core ML ---
torch==2.3.1
torchaudio==2.3.1
transformers==4.51.3
numpy==1.26.4
hydra-core==1.3.2
HyperPyYAML==1.2.3
omegaconf==2.3.0

# --- Audio I/O / DSP ---
librosa==0.10.2
soundfile==0.12.1
pyworld==0.3.4

# --- ONNX runtime (GPU on linux, CPU on win/mac) ---
onnx==1.16.0
onnxruntime-gpu==1.18.0; sys_platform == 'linux'
onnxruntime==1.18.0; sys_platform != 'linux'

# --- Modelscope + HF (huggingface_hub is transitive) ---
modelscope==1.20.0

# --- Linux-only performance / Windows-incompatible ---
tensorrt-cu12==10.13.3.9; sys_platform == 'linux'
deepspeed==0.15.1; sys_platform == 'linux'
ttsfrd; sys_platform == 'linux'

# --- Text normalization fallback used on non-Linux ---
wetext==0.0.4

# --- Misc helpers actually used by CosyVoice runtime ---
tensorboard
tqdm
matplotlib
WeTextProcessing
jieba
pypinyin
pyarrow
av
pydub
PyYAML
safetensors
einops
timm
kaldi-native-fbank
kaldifeat
silero-vad
setuptools>=65.0.0
inflect
onnx2torch
typeguard
gdown
sox
absl-py
loguru
langdetect
praat-parselmouth
Levenshtein
scipy
gradio
huggingface_hub
